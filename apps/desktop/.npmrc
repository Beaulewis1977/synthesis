# Electron Builder configuration for pnpm
# Prevents electron-builder from following pnpm symlinks which causes build errors

# Use shamefully-hoist to flatten node_modules for electron-builder
shamefully-hoist=true

# Disable strict peer dependencies for electron-builder compatibility
strict-peer-dependencies=false
