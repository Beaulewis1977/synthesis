/**
 * Knowledge Graph Types for Graph Debug UI
 * GPT Phase 2: Graph Retrieval & Context Expansion
 *
 * Re-exports core types from @synthesis/shared and adds UI-specific types.
 */

// Re-export shared types for convenience
export type {
  KnowledgeNode,
  KnowledgeNodeType,
  KnowledgeEdge,
  KnowledgeEdgeType,
} from '@synthesis/shared';
