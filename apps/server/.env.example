# =============================================================================
# SYNTHESIS SERVER - ENVIRONMENT CONFIGURATION
# =============================================================================
# NOTE: You typically don't need this file!
# The server inherits all variables from the root .env file.
# Only create apps/server/.env if you need to OVERRIDE specific values.
# =============================================================================

# Example: Override the database URL for server-specific testing
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/synthesis_test
